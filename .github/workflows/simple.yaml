name: DGDatabase

on: [push]

jobs:
  runDatabaseQuery:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: multiline script
        run: |
          pwd
          jmeter -n -t DailyGenixDatabase.jmx

